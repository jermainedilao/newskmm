object Android {
    private const val buildGradleVersion = "7.0.0"
    private const val buildKonfigVersion = "0.9.0"

    const val buildGradle = "com.android.tools.build:gradle:$buildGradleVersion"
    const val buildKonfig = "com.codingfeline.buildkonfig:buildkonfig-gradle-plugin:$buildKonfigVersion"
}
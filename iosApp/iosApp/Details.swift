import SwiftUI
import shared
import Combine

struct Details : View {
    var body: some View {
        Text("Hello, Details!")
            .navigationTitle("Details")
            .navigationBarTitleDisplayMode(.inline)
    }
}
